# Delete a file  
rm file.txt

# Delete multiple files  
rm file1.log file2.tmp notes.md

# Recursively delete a directory  
rm -r old_project/

# Delete nested directories and their contents  
rm -r cache/ temp/ logs/

# Delete a file or directory (with -r, handles both types)  
rm -r item/